[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

# SPA redirect — toutes les routes → index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
